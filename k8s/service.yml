apiVersion: v1
kind: Service
metadata:
  name: person-service
  labels:
    app: person-service
spec:
  type: ClusterIP
  selector:
    app: person-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
